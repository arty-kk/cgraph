# SaaS Navigation Map

> **Generated by** `SaaS Navigation Map` prompt.
> **Last regenerated:** 2026-05-23
> **Source state:** abdbacd
> **Regenerate:** re-run the prompt; this file is overwritten. Stable IDs are preserved where code identity has not changed.

## Contents

- [Summary](#summary)
- [Diagram](#diagram)
- [Auth and onboarding entry points](#auth-and-onboarding-entry-points)
- [Layouts](#layouts)
- [Routes](#routes)
- [Modals, drawers, dialogs](#modals-drawers-dialogs)
- [Forms](#forms)
- [Tables and lists](#tables-and-lists)
- [Navigation surfaces](#navigation-surfaces)
- [Role × Route matrix](#role--route-matrix)
- [Role × Action matrix](#role--action-matrix)
- [Plan × Feature matrix](#plan--feature-matrix)
- [Cross-cutting](#cross-cutting)
- [Reachability](#reachability)
- [Unknowns](#unknowns)
- [Assumptions](#assumptions)

## Summary

- Conditional sections applied: multi-tenant (organization selection), auth-and-onboarding (onboarding modal only)
- Conditional sections not detected: RBAC, plan-gating, URL-level auth routes
- Routes: 1 (public: 1, authenticated: 0, admin: 0)
- Layouts: 1
- Modals / drawers: 7
- Forms: 7
- Tables / lists: 7
- Roles detected: not detected in frontend route surface
- Plans detected: not detected
- Routes with no inbound navigation detected: 0
- Source files scanned: `frontend/src/main.tsx`, `frontend/src/ui/App.tsx`, `frontend/src/ui/components/ProjectsSidebar.tsx`, `frontend/src/ui/components/CommandPalette.tsx`, `frontend/index.html`
- Tests / fixtures consulted: none

## Diagram

```mermaid
graph LR
  Browser[/GET "/"/] --> R001[R-001 App]
  R001 --> M001[M-001 Unsaved changes]
  R001 --> M002[M-002 Restore draft]
  R001 --> M003[M-003 Project docs]
  R001 --> M004[M-004 Getting started]
  R001 --> M005[M-005 Sidebar help]
  R001 --> M006[M-006 Delete project]
  R001 --> M007[M-007 Command palette]
```

## Auth and onboarding entry points

- Onboarding entry is local-storage driven and project-scoped: `cs.onboarding.seen.<projectId>` opens the “Getting started” modal when a project is active and the flag is not set. Source: `frontend/src/ui/App.tsx:62-68`.
- No sign-in/sign-up/password-reset/SSO route handlers are implemented in frontend routing; app mounts directly to `App` on `/`. Source: `frontend/src/main.tsx:18-23`, `frontend/index.html:10`.

## Layouts

<a id="l-001"></a>
### L-001 — `App`

```text
ID:               L-001
Name:             App
Source:           frontend/src/ui/App.tsx:20 — App
Contains:         left panel (ProjectsSidebar), graph/editor center workspace,
                  right panel (NodePanel), notifications overlay, command palette,
                  modal host
Used by:          R-001
Sidebar items:    Implemented inside ProjectsSidebar (Explorer / Manage tabs and controls)
Top-bar items:    none (no dedicated top navigation bar component)
Notes:            Workspace toggles between graph and editor views, but URL path stays unchanged.
```

## Routes

### Public

<a id="r-001"></a>
#### R-001 — `/`

```text
ID:               R-001
Path:             /
Page component:   App
Source:           frontend/src/main.tsx:21 — App
Layout:           L-001 (App)
Tenant scope:     global (organization picker exists, but no tenant path segment)
Auth:             none (no frontend route guard)
Required roles:   —
Required plan:    —
Permission keys:  —
Feature flags:    semantic search availability is capability-gated via API key / embeddings state
Entry from:       External browser entry via index.html bootstrap
Navigates to:     No URL route transitions; in-app state transitions only
States rendered:  loading (orgs/projects/search/docs/task statuses), empty (editor empty state,
                  no matches, no docs), error (search/docs/task/project errors), custom stale-graph warning
Modals:           M-001, M-002, M-003, M-004, M-005, M-006, M-007
Forms:            F-001, F-002, F-003, F-004, F-005, F-006, F-007
Tables / lists:   T-001, T-002, T-003, T-004, T-005, T-006, T-007
Notes:            This SPA uses a single mount route; navigation is view-state driven.
```

## Modals, drawers, dialogs

<a id="m-001"></a>
### M-001 — `Unsaved changes` confirm modal

```text
ID:               M-001
Name:             Unsaved changes modal
Source:           frontend/src/ui/App.tsx:820-854 — Modal
Kind:             modal
Purpose:          Confirm save/discard/cancel when unsaved changes block action
Opened from:      R-001 (app.confirmOpen state)
Opens for:        File editor unsaved change flows
Forms:            —
Confirmation?:    yes (destructive)
Notes:            Title switches to "Reload file?" for reload-specific reason.
```

<a id="m-002"></a>
### M-002 — `Restore draft?`

```text
ID:               M-002
Name:             Restore draft modal
Source:           frontend/src/ui/App.tsx:856-882 — Modal
Kind:             modal
Purpose:          Restore or discard local draft
Opened from:      R-001 (app.draftRestore presence)
Opens for:        Draft recovery
Forms:            —
Confirmation?:    yes (destructive on discard)
Notes:            Local-only state based on stored draft data.
```

<a id="m-003"></a>
### M-003 — `Project docs`

```text
ID:               M-003
Name:             Project docs modal
Source:           frontend/src/ui/App.tsx:884-1035 — Modal
Kind:             modal
Purpose:          View/rebuild generated project documentation markdown
Opened from:      R-001 via sidebar "Docs" and command palette "Open docs"
Opens for:        Active project docs
Forms:            F-006
Confirmation?:    no
Notes:            Includes external/file link handling and markdown rendering.
```

<a id="m-004"></a>
### M-004 — `Getting started`

```text
ID:               M-004
Name:             Getting started modal
Source:           frontend/src/ui/App.tsx:1037-1136 — Modal
Kind:             modal
Purpose:          Project onboarding checklist/tutorial
Opened from:      R-001 when onboarding flag is unseen for active project
Opens for:        Onboarding actions (scan, open palette, open editor)
Forms:            —
Confirmation?:    no
Notes:            Multi-step wizard with progress and "Don’t show again".
```

<a id="m-005"></a>
### M-005 — `Help` modal (projects/graph/search)

```text
ID:               M-005
Name:             Sidebar help modal
Source:           frontend/src/ui/components/ProjectsSidebar.tsx:656-695 — Modal
Kind:             modal
Purpose:          Inline help for projects, graph settings, and search modes
Opened from:      R-001 via ? help buttons in ProjectsSidebar section headers
Opens for:        Contextual user guidance
Forms:            —
Confirmation?:    no
Notes:            Content variant depends on selected help topic.
```

<a id="m-006"></a>
### M-006 — `Delete project?`

```text
ID:               M-006
Name:             Delete project confirmation
Source:           frontend/src/ui/components/ProjectsSidebar.tsx:696-723 — Modal
Kind:             modal
Purpose:          Confirm irreversible project deletion
Opened from:      R-001 via Delete button in project management area
Opens for:        Active project delete action
Forms:            —
Confirmation?:    yes (irreversible)
Notes:            Warns about deleting graph, contracts, run history.
```

<a id="m-007"></a>
### M-007 — `CommandPalette`

```text
ID:               M-007
Name:             CommandPalette
Source:           frontend/src/ui/components/CommandPalette.tsx:81 — CommandPalette
Kind:             dialog
Purpose:          Keyboard-driven command/file/project navigation hub
Opened from:      R-001 via app.setPaletteOpen(true) call-sites
Opens for:        Project selection, file jump, editor and graph actions
Forms:            F-007
Confirmation?:    no
Notes:            Rendered through shared Modal component in CommandPalette implementation.
```

## Forms

<a id="f-001"></a>
### F-001 — `Project create` form group

```text
ID:               F-001
Name:             Project creation controls
Source:           frontend/src/ui/components/ProjectsSidebar.tsx:391-434 — JSX controls
On routes:        R-001
On modals:        —
Fields:           newName:text:required(trimmed), newArchive:file:optional,
                  newPath:text:conditionally required when no archive and local root allowed
Submit endpoint:  onCreateProject() action
Async validation: none
Multi-step?:      no
Notes:            Local root path can be disabled by backend capability flag.
```

<a id="f-002"></a>
### F-002 — `Graph settings` form group

```text
ID:               F-002
Name:             Graph controls
Source:           frontend/src/ui/components/ProjectsSidebar.tsx:436-537 — JSX controls
On routes:        R-001
On modals:        —
Fields:           mode:select(required), N:number(100..20000), hops:number(1..6),
                  localMax:number(50..2000)
Submit endpoint:  onScan()/onRefresh() actions
Async validation: none
Multi-step?:      no
Notes:            Numeric input clamped via clampInt.
```

<a id="f-003"></a>
### F-003 — `Sidebar file search`

```text
ID:               F-003
Name:             File search controls (manage tab)
Source:           frontend/src/ui/components/ProjectsSidebar.tsx:539-579 — JSX controls
On routes:        R-001
On modals:        —
Fields:           semanticSearchEnabled:boolean, searchQuery:text(required for submit)
Submit endpoint:  onSearchNodes(searchQuery)
Async validation: none
Multi-step?:      no
Notes:            Availability messages for missing API key/embeddings/no embeddings.
```

<a id="f-004"></a>
### F-004 — `Editor search panel`

```text
ID:               F-004
Name:             Editor-side project text search
Source:           frontend/src/ui/App.tsx:506-549 — JSX controls
On routes:        R-001
On modals:        —
Fields:           query:text, prefix:text(optional), caseSensitive:boolean
Submit endpoint:  app.onSearchText(app.textSearchQuery)
Async validation: none
Multi-step?:      no
Notes:            Enter key submits query.
```

<a id="f-005"></a>
### F-005 — `Organization/project selector`

```text
ID:               F-005
Name:             Context selectors
Source:           frontend/src/ui/components/ProjectsSidebar.tsx:320-379 — JSX controls
On routes:        R-001
On modals:        —
Fields:           organizationId:select, projectId:select
Submit endpoint:  onSelectOrg(id), onPickProject(project)
Async validation: none
Multi-step?:      no
Notes:            Organization selector hidden when only one org is present.
```

<a id="f-006"></a>
### F-006 — `Project docs actions`

```text
ID:               F-006
Name:             Docs modal actions
Source:           frontend/src/ui/App.tsx:886-902 — action buttons
On routes:        —
On modals:        M-003
Fields:           none (action-only)
Submit endpoint:  app.buildDocs(), app.loadDocs()
Async validation: none
Multi-step?:      no
Notes:            Button disabled states encode busy/loading gating.
```

<a id="f-007"></a>
### F-007 — `Command palette query`

```text
ID:               F-007
Name:             Command palette input
Source:           frontend/src/ui/components/CommandPalette.tsx:129-205 — state/effects
On routes:        —
On modals:        M-007
Fields:           query:text (search active when length >=2 and active project exists)
Submit endpoint:  searchNodes(projectId, query, 20) and in-memory command filtering
Async validation: none
Multi-step?:      no
Notes:            Debounced async search with sequence cancellation.
```

## Tables and lists

<a id="t-001"></a>
### T-001 — `Background tasks list`

```text
ID:               T-001
Name:             Background tasks status list
Source:           frontend/src/ui/App.tsx:272-314 — taskStatuses.map
On route:         R-001
Columns:          label, kind, status, error, dismiss action
Filters:          —
Sort fields:      —
Search:           —
Pagination:       none
Bulk actions:     clear completed
Row actions:      dismiss
States:           visible/non-visible, error-indicator per row
Notes:            Overlay appears only when taskStatuses length > 0.
```

<a id="t-002"></a>
### T-002 — `Project selector list`

```text
ID:               T-002
Name:             Projects dropdown list
Source:           frontend/src/ui/components/ProjectsSidebar.tsx:343-380 — projects.map
On route:         R-001
Columns:          project name (option)
Filters:          —
Sort fields:      —
Search:           —
Pagination:       none
Bulk actions:     —
Row actions:      activate project
States:           loading, empty (No projects), disabled while busy
Notes:            Implemented as select options rather than table rows.
```

<a id="t-003"></a>
### T-003 — `Path/semantic search results`

```text
ID:               T-003
Name:             Sidebar search results list
Source:           frontend/src/ui/components/ProjectsSidebar.tsx:581-650 — searchResults/searchSemanticResults maps
On route:         R-001
Columns:          path, language/fan-in/fan-out or semantic score/snippet
Filters:          semantic toggle
Sort fields:      backend-defined
Search:           searchQuery
Pagination:       none
Bulk actions:     —
Row actions:      select path
States:           empty, loading(searchBusy), fallback path-search label, unavailable semantic reasons
Notes:            Two render modes: lexical and semantic.
```

<a id="t-004"></a>
### T-004 — `Editor text search results`

```text
ID:               T-004
Name:             Editor search match list
Source:           frontend/src/ui/App.tsx:565-587 — textSearchResults.map
On route:         R-001
Columns:          path, line, column, snippet, truncated flag
Filters:          prefix, caseSensitive
Sort fields:      backend-defined
Search:           textSearchQuery
Pagination:       none
Bulk actions:     —
Row actions:      open file at match position
States:           empty("No matches yet"), loading(textSearchBusy), error(textSearchError), metadata warning
Notes:            Lives in editor left pane search tab.
```

<a id="t-005"></a>
### T-005 — `Command palette item list`

```text
ID:               T-005
Name:             Palette item list
Source:           frontend/src/ui/components/CommandPalette.tsx:207-335 — CommandPalette#commandItems
On route:         R-001
Columns:          title, subtitle, group, hint, disabled
Filters:          query text
Sort fields:      grouped/constructed order
Search:           query
Pagination:       none
Bulk actions:     —
Row actions:      execute command/open file/open project
States:           empty(no match), loading(filesBusy), active selection index
Notes:            Combines static command items and async file search results.
```

<a id="t-006"></a>
### T-006 — `Onboarding steps list`

```text
ID:               T-006
Name:             Onboarding steps
Source:           frontend/src/ui/App.tsx:189-233,1071-1113 — onboardSteps map
On route:         R-001
Columns:          step number, title, description, tip, optional action
Filters:          —
Sort fields:      fixed sequence
Search:           —
Pagination:       none
Bulk actions:     —
Row actions:      per-step CTA action
States:           active/inactive step visuals
Notes:            Rendered within onboarding modal.
```

<a id="t-007"></a>
### T-007 — `Docs markdown tables`

```text
ID:               T-007
Name:             Markdown-rendered docs tables
Source:           frontend/src/ui/App.tsx:939-952 — ReactMarkdown table renderers
On route:         R-001
Columns:          content-defined
Filters:          —
Sort fields:      —
Search:           —
Pagination:       none
Bulk actions:     —
Row actions:      linked file/url navigation where present
States:           empty(no docs markdown), stale-warning banner when build failed
Notes:            Structure depends on generated markdown payload.
```

## Navigation surfaces

- **Primary URL surface:** single route `/` mounted by Vite entrypoint; no client-side router detected (`frontend/index.html:10` and `frontend/src/main.tsx:18-23 — main render`).
- **Left panel / Sidebar (`ProjectsSidebar`): (`frontend/src/ui/App.tsx:320-373 — App`, `frontend/src/ui/components/ProjectsSidebar.tsx:72-728 — ProjectsSidebar`)**
  - Tabs: `Explorer`, `Manage`.
  - Manage controls: org selector, project selector/delete, add project, graph controls, file search.
  - Explorer controls: tree navigation and docs button.
- **Center workspace switches:** `graph` vs `editor` modes, controlled by `workspaceView` state (`frontend/src/ui/App.tsx:396-705 — App`).
- **Right panel (`NodePanel`):** graph-node context and run/action controls (`frontend/src/ui/App.tsx:708-763 — App`).
- **Command palette:** grouped commands (`Project`, `Graph`, `UI`, `Editor`, `Selection`, `Navigation`) and file/project quick-jump (`frontend/src/ui/components/CommandPalette.tsx:8,207-335 — CmdGroup/CommandPalette#commandItems`).
- **Breadcrumb behavior:** not detected as explicit breadcrumb component/logic.

## Role × Route matrix

Not applicable: RBAC route gating was not detected in the frontend route surface.

## Role × Action matrix

Not applicable: RBAC action gating was not detected in the frontend route surface.

## Plan × Feature matrix

Not applicable: plan/subscription feature gating was not detected in the frontend route surface.

## Cross-cutting

- **Permission helpers:** not detected in frontend route/components.
- **Feature-flag wrappers:** semantic search toggle is capability-conditioned by `semanticSearchUnavailableReason` and backend embedding availability states (`frontend/src/ui/components/ProjectsSidebar.tsx:154-160,542-558 — ProjectsSidebar`).
- **Plan-gating components:** not detected.
- **Shared empty / error / loading components:** no centralized shared component; states are rendered inline in `App` and `ProjectsSidebar` (`frontend/src/ui/App.tsx:565-567,596-628,915-917 — App`; `frontend/src/ui/components/ProjectsSidebar.tsx:310-347,652 — ProjectsSidebar`).
- **Localization / i18n:** not detected.
- **Route guards:** not detected; app mounts directly without protected route wrappers.

## Reachability

- **Routes with no inbound navigation detected:** none (single `/` route entered externally).
- **Routes reachable only via deep link / direct URL:** none.
- **Modals that no route opens:** none statically; each modal has an observed opener/state trigger in R-001.

These are recorded as facts, not as defects. Confirming reachability requires runtime context this prompt does not have.

## Unknowns

- Whether backend/API enforces auth/roles/plans for actions invoked from this SPA cannot be determined from frontend route structure alone (frontend evidence only: `frontend/src/main.tsx:18-23 — main render`, `frontend/src/ui/App.tsx:20-1165 — App`).
- Exact command palette visual list rendering and ordering at runtime are partially inferred from static code; dynamic ordering also depends on query state and async search responses (`frontend/src/ui/components/CommandPalette.tsx:129-205,207-335 — CommandPalette`).
- Any deployment-time reverse proxy path rewriting (subpath hosting) is unknown from the scanned frontend files (`frontend/index.html:10`, `frontend/nginx.conf:1-18`).

## Assumptions

- The discoverable user-facing surface for this task is the frontend SPA (`frontend/`) rather than backend FastAPI endpoints.
- In-app workspace mode transitions (graph/editor) were treated as navigation surfaces, not separate URL routes.
- Lists rendered as selects or mapped button collections were included as “tables/lists” per prompt scope.
